# requirements-train.txt — install inside Colab/Kaggle for LoRA training
# (train.py) and to push the trained adapter to Hugging Face Hub. The
# deployed feedback-service never needs this — it only calls the Hugging
# Face Space via gradio_client (SPACE_ID). See app/serving/hf_space/DEPLOY.md
# for the Space's own (separate) requirements.txt.
-r requirements.txt
# Exact-pinned (not ranges) — these versions are tested to work together on
# Colab's T4 runtime. Newer trl/peft/transformers pull in optional
# integrations (liger-kernel, torchao) that conflict with Colab's
# pre-installed packages, so don't loosen these without re-testing.
transformers==4.44.2
torch
accelerate==0.33.0
peft==0.12.0
trl==0.9.6
datasets==2.21.0
huggingface_hub
